package triplestar.mixchat.domain.ai.chatbot;

import org.springframework.stereotype.Service;
import triplestar.mixchat.domain.chat.chat.entity.AIChatRoom;

@Service
public class RagVectorService {
    public String chat(Long userId, AIChatRoom roomId, String userMessage, String persona) {
        return "";
    }
}
