spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev} # 배포시 dev만 prod로 변경
    include:
      - secret
      - s3
  config:
    import: optional:file:.env.${spring.profiles.active}.properties
  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://mysql:3306/${MYSQL_DATABASE}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

jwt:
  access-token-expiration-seconds: "#{60 * 15}"
  refresh-token-expiration-seconds: "#{60 * 60 * 24 * 7}"
  access-secret: "localtestjwtsecret123456789012345678901234" # 삭제예정
  refresh-secret: "localtestjwtsecret123456789012345678901234" # 삭제예정
redis:
  prefix:
    refresh-token: "refresh:"
cookie:
  domain: ${DOMAIN_NAME:mixchat.shop}

swagger:
  redirect:
    # 💡 "/"로 들어왔을 때 최종적으로 리다이렉트될 Swagger UI 경로
    path: /swagger-ui/index.html